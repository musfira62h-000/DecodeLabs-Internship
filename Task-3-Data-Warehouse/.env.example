# Connect through SSH tunnel (recommended for private RDS):
#   ssh -i your-key.pem -L 3307:YOUR-RDS-ENDPOINT:3306 ec2-user@BASTION-PUBLIC-IP -N
# Then use localhost + local tunnel port:

DB_HOST=127.0.0.1
DB_PORT=3307
DB_NAME=internsdb
DB_USER=admin
DB_PASSWORD=YourStrongPassword123!
